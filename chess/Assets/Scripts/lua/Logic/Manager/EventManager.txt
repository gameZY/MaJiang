--事件管理器
Event = require 'events'

EventManager = {}

function EventManager.RegisterEvent(index,callback)
    Event.AddListener(index, callback);
end

function EventManager.InvokeEvent(index,data)
    Event.Brocast(index,data);
end

function EventManager.UnRegisterEvent(index)
    Event.RemoveListener(index);
end